import type { PhotoInfo } from "@tokimo/sdk";

/** Placeholder for future AI toolbar buttons. */
export function PhotoAiToolbarButtons({
  photo: _photo,
}: {
  photo: PhotoInfo;
}) {
  void _photo;
  return null;
}
